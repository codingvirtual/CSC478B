/**
 * Core classes necessary for the base operations involved in Mirror including FileSet (defines properties
 * and methods for manipulating the settings for a Mirror operation) as well the FileOps class (provides
 * standard mirroring operations).
 */

package core;